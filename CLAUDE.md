# Portfolio Project - Claude Guidelines

> このドキュメントは Claude Code がこのリポジトリで作業する際の最上位ルールです。
> 各章の優先順位は番号順です。**§0（最優先原則）は他のすべてのルールに優先します。**

---

## 0. 最優先原則（Safety First）

**スピードよりも、正確性と安全性の確認を常に優先する。** あなたは本番環境を操作する責任あるエンジニアとして振る舞うこと。

### 0-1. ツール出力の捏造（ハルシネーション）の禁止
- ツールの実行結果（JSON・標準出力・エラーメッセージなど）を、推測や模倣で作成（捏造）して出力することを **厳禁** とする。実際に実行して得た結果のみを報告すること。
- ツール呼び出しのマークアップ／構文エラーが発生した場合、勝手に成功と仮定しない。正しい構文で再実行するか、即座にユーザーにエラーを報告すること。
- **不可逆な操作の検証義務:** 本番環境（Production）へのマイグレーション（`apply_migration` など）や DB への書き込みなど取り消せない操作を行った場合、実行後に必ず **独立した検証コマンド**（マイグレーション一覧の取得、DB クエリ等）を別途実行し、物理的な適用結果を確認・報告すること。

### 0-2. ファイルの誤削除・環境破壊の防止
`.env` の読み取り・force push・システムディレクトリの再帰削除はグローバルの deny / `~/.claude/hooks/guard.sh` が止める。門が見ていないのは **git 管理外ファイルの上書き** なので、そこだけ人が守る。
- `.gitignore` 指定ファイル・`.claude/` などのツール設定・`.env` を、明示的な許可なく削除・初期化・上書きすることを **厳禁** とする。
- 削除・置換の前に「git で復元できないローカル設定か」を必ず確認する。復元できないものは編集前にバックアップを取る。

### 0-3. 判断に迷ったときの既定動作
- ルールの適用範囲やリスクの判定に迷う場合、または本ドキュメントに明記がないグレーゾーンの場合は、**必ず安全側（§3 の「要承認」側）に倒し、作業を停止してユーザーに確認する。**

### 0-4. 新しい作業依頼を受けたときの入口
- **新しい作業依頼を受けたら、着手前に必ず `.claude/skills/director`（受付席）を通す。** §0-1 / §2-2 / §3 / §4 / §5 / §6-2 の各ゲートを、director が一括判定し依頼書にまとめる。
- 各ゲートは director を通さない場面（合意済み依頼への追加指示など）でも常に有効。director は判定を前倒しするだけで、適用範囲を狭めない。director を読み込めない場合はその旨を報告し、各ゲートを手動で通す。
- 追跡する: `.claude/agents/` `.claude/skills/` `.claude/hooks/` `.claude/settings.json`。追跡しない: `.claude/settings.local.json` `.claude/launch.json` `.claude/worktrees/`。

### 0-5. 応答の閉じ方（クラウドセッションでも適用）
`~/.claude/CLAUDE.md` はこの Mac のローカルセッションにしか届かない。claude.ai/code のクラウドセッション（ブランチ `claude/…`）はリポジトリ内の設定しか読まないため、閉じ方のルールをここにも置く。
- **本文の最後は必ず `**完了**`（何が終わったか1行）か `**次のアクション**`（誰が何をするか1つ）で閉じる。** 判断がユーザーの手にあるときだけ AskUserQuestion で1問聞く。
- 禁止: 「必要でしたら作ります」「どうしますか?」のような申し出・問いかけで終える。前置き・結び・謝罪を書かない。実行結果と「未確認」の明記は省かない。
- 判定は `.claude/hooks/close-gate.sh`（Stop hook）が機械的に行い、閉じていなければ差し戻す。

### 0-6. 実行面のズレ確認（クラウドセッションでも適用）
着手時に「今いる面（ローカル / クラウド）が作業に合っているか」を1行で判定し、合っていれば「実行面: ローカル（妥当）」とだけ書く。クラウドセッション（ブランチ `claude/…`）で、必要な環境変数（Supabase 等）・MCP（Figma）・`~/.claude` の hook が無いと分かったら、推測で埋めず **ローカルでの続行を「次のアクション」に書く**。ローカルからクラウドへ委譲する基準は director のゲート 7（§6）。

---

## 1. プロジェクト概要

Next.js 製のポートフォリオサイト。構成は `app/`（ページ）・`components/` と `src/components/`（UI）・`lib/`（`design-tokens.ts` 等）・`public/`（静的アセット）。詳細はリポジトリを見る。

---

## 2. 実行ポリシー（自動実行 vs 要承認）

タスク実行時、開発者の許可を毎回求めず自律的に実行してよい作業と、必ず明示的な承認を要する作業を以下に定義する。
**いずれに該当するか迷う場合は、§0-3 に従い「要承認」側に倒すこと。**

### 🟢 2-1. 自動実行を許可する作業（Auto-Run Allowed）
2-2 に当たらず、かつ `git diff --stat` の変更ファイルが **`components/` 配下で1つ以内** に収まるもの。許可を求めず実行してよい。

- 門の実行（`npm run check`）、ドキュメント・コメント・型注釈の更新、スタイルの微調整、単一ファイルに閉じたバグ修正。
- ※ `app/globals.css` の `@theme` / `lib/design-tokens.ts` を変更する場合は §4 の更新義務が同時に発生する。

### 🔴 2-2. 必須確認・承認を要する作業（Manual Approval Required）
**コマンド実行や破壊的なファイル書き換えの前に、変更内容のサマリーと理由を提示し承認を得る。**

- **依存関係:** `package.json` へのパッケージ追加・更新（`npm install` / lockfile 変更）。
- **スキーマ・DB:** Supabase / SQL のマイグレーション生成・実行、DB 書き込み。
- **認証・セキュリティ:** Supabase Auth、RLS ポリシー、API ルートの認証・認可ロジック。
- **環境変数:** `.env` の変更、新規環境変数の導入提案（※シークレットはユーザーに入力させない）。
- **ビルド設定:** `tsconfig.json` / `next.config.ts` / `.github/workflows/*` / `.husky/*` / `package.json` の `scripts`。
- **影響範囲が広い変更:** `git diff --stat` の変更ファイルが **`components/` 配下で2つ以上**、または既存インターフェース・API 仕様を破壊する修正。

---

## 3. Git 運用ルール

規約は門が機械的に判定する。ここには**門が判定できないこと**だけを書く。

| 規約 | 止める門 |
|------|----------|
| コミット形式 `<type>: <description>`（type: `feat` / `fix` / `refactor` / `style` / `chore` / `docs`） | commitlint（`.husky/commit-msg`） |
| ブランチ名 `<type>/<yyyymmdd>-<description>` | `.husky/pre-push` |
| `temp/` ブランチでのコミット | `.claude/hooks/block-temp-commit.sh`（Claude Code / Codex の両経路） |

- **ブランチをどう切るかの分岐**は `.claude/skills/director` の Phase C ゲート3 が判定する。ここには書かない。
- 作業内容が指示に含まれていない場合**のみ** `temp/<yyyymmdd>-work` を作る。内容が決まっているなら適切な type を使う。コミット前に `git branch -m <type>/<yyyymmdd>-<description>` でリネームすること（門が止めるが、非対話セッションでは hook が読み込まれない — VERIFIED 2026-09-07）。

### 3-1. 原則禁止事項
- **main ブランチでの直接作業**（作業指示があった場合は、そのまま続行してよいか必ず確認すること）。
- **main ブランチの変更をそのまま push すること。**
- **PR のマージ**（ユーザーの明示的な指示なく `gh pr merge` などを実行することを厳禁とする）。

---

## 4. デザインシステム連動ルール（Style Guide Auto-Update）

以下に影響する変更を行う場合、**同じタスク内で必ずスタイルガイド** (`app/styleguide/StyleguideLayout.tsx`) **も更新する。**

| トリガー | 必須対応 |
|---------|----------|
| `components/` に新規ファイル追加 | `ComponentsSection` に import と `<ComponentPreview>` を追加 |
| 既存コンポーネントの props / variants 変更 | 対応する `<ComponentPreview>` の例を更新 |
| `lib/design-tokens.ts` のトークン追加 / 削除 | `StyleguideLayout.tsx` の該当セクション（Colors / Tokens）を更新 |
| `app/globals.css` の `@theme` ブロック変更 | スタイルガイドの色 / radius スウォッチが値を反映しているか確認 |

更新が不要なのは、コンポーネント内部ロジックのみの変更（prop API・見た目に変化がない）と、デザインシステムと無関係な `app/` 配下ページの変更。

---

## 5. Figma 確認ルール（Design Source of Truth）

Figma データの確認・デザイン変更を伴う作業では、判断を下す前に必ず次の3つを済ませる。**表層のインスタンス情報だけで実装に進まない。**

1. **ライブラリ突き合わせ（必須）**: 作業のたびに [Library](https://www.figma.com/design/KpNwkdFy1usaO1sBR0dycv/Library?node-id=0-1&t=NBs3RMQLsi9pubO0-1) を確認し、実装側（`lib/design-tokens.ts` / `app/globals.css` / 各コンポーネント）との差分を洗う。差分は実装へ反映する。
2. **マスターコンポーネントまでの遡及（必須）**: インスタンスから定義元までさかのぼる。
3. **不明点は着手前に確認（必須）**: 抜けている pattern や足りない方針があれば、実装方針をユーザーに確認してから実装する。曖昧なまま着手しない（§0-3）。

1・2 の取得対象は例示に限定せず、**取得できる限りの定義・数値まで** 遡る: 値・トークン・バリアント・スタイル（色 / タイポグラフィ / radius / shadow / spacing）、レイアウト数値（Auto Layout の方向・gap・padding・整列・サイズ制約・余白・寸法）、Property / Variant / Variable とバインド先、ネストされた子要素の構造と数値、命名・階層・状態・制約・意図。

**突き合わせの手段**: 1・2 は **`/figma-component-review`** で行う。目視より漏れが少ない。

- **`token` モードから回す。** トークンの値がズレたままコンポーネントを見ると、指摘が全部同じ原因に収束して読めなくなる。
- 前提はリポジトリ直下の `.design-system-context.yml` から読まれる。無ければ skill が5問聞く。
- Figma のノードIDが要るときは skill 側から「Copy link to selection」の URL を求める。**変数コレクション全体を列挙する手段が無い**ため、ここは自動化できない。
- 見た目・UX のレビューは `/design-review`。担当が違うので取り違えない。

---

## 6. オーケストレーション（席の運用 / モデル切替 / コストガード）

検証駆動のオーケストレーション構成。席の定義は `.claude/agents/*.md`、失敗パターンは `.claude/skills/coding-lessons` / `research-lessons`、学びは `learnings/` に置く。
受付は `.claude/skills/director`（サブエージェントではなくメインセッションで動く skill。依頼を受けた時点で読み込む）。受付段階で起動してよいのは `Explore` と `scope-planner` のみ。他の席は依頼書に合意が出てから起動する。

### 6-1. 席の役割分担
| 席 | model / 実体 | 役割 |
|----|-------|------|
| メインセッション | opus（`~/.claude/settings.json` の `"model": "opus"` が既定。手動切替は不要） | 計画専任。分解・統合・エスカレーション判断のみ。実装はしない |
| `director` | skill・メインセッション上で動作 | 依頼の受付。ゲート（§0-1 / §2-2 / §3 / §4 / §5 / §6-2）の一括判定 → 依頼書 → 合意まで。実装・設計はしない |
| `Explore`（組込） | — | 所在特定（読取のみ） |
| `scope-planner` | opus | 依頼を境界明確な要件へ縮約 |
| `architect` | opus | 方式・トレードオフ・手順の設計（コードは書かない） |
| `builder` | sonnet | 設計どおり実装＋検証（設計外判断は止めて返す） |
| `qa-verifier` | opus | まっさら文脈で破壊テスト（境界値・異常系） |
| `fable-verifier` | fable | 高リスク第2意見（下記3条件のときだけ） |
| `shipper` | haiku | コミット・CI・雑務 |

原則: judgment 席=opus、手続き席=sonnet/haiku。既定はメイン opus / 実装 sonnet の固定席。

### 6-2. fable エスカレーション・ルール
次の3条件のいずれかに該当したときだけ fable 席（`fable-verifier`）を呼ぶ。該当しなければ呼ばない。
- **① 設計の山場**: あいまい・トレードオフだらけで後続実装全体が崩れうる分岐
  → opus と fable に独立に設計させ、相互の答えを見せずに統合
- **② 失敗コスト大**: 不可逆、または影響範囲が広い判断
  → fable を第2意見として並列に立てる
- **③ プレイブック非該当の異常**: lesson/skill に該当せず「何かおかしい」or 未知構造
  → 問題構造の把握だけを fable に

### 6-3. fable コスト・ガード（発火前に適用）
境界のある単一の問いに縮約して渡す（全文脈は渡さない）。1タスク最大1回。effort は既定 `high`、`max` は「設計の山場かつ不可逆」のみ。workflow 使用時は `budget.remaining() < 全体の30%` で停止し opus にフォールバック。超過時も opus の第2意見で代替する。

> 注記（VERIFIED 2026-07-21）: このアカウントで `model: fable`（claude-fable-5）は実起動可能。使えない環境では `fable-verifier` を `model: opus` に読み替え、「最上位モデルの第2意見席」として運用する。
