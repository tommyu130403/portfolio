-- ============================================================================
-- Drop legacy public.skill_tools (2026-06-02)
--   カード単位ツール（skill_tools）はスキルセクションのアコーディオン化で
--   skill_experience_tools（行単位）へ移行済み。公開ページ・admin とも未参照、
--   行も 0 件のため廃止する。
--
--   ※ baseline 移行（20260531120000）が CREATE しているため、fresh reset では
--     「作成→本移行で削除」となり最終的にテーブルは存在しない。
-- ============================================================================

DROP TABLE IF EXISTS public.skill_tools CASCADE;
